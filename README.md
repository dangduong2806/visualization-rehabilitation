**Tải tập data về, giải nén vào folder hiện tại, chỉnh sửa đường dẫn trực tiếp trong file dataloader.py**  

**Chạy python main.py để train** 

**Chỉnh sửa số lượng epoch, batch size, v.v trong file config**
